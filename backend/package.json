{
  "name": "evoting-system",
  "version": "1.0.0",
  "description": "Sistema de votação eletrônica usando blockchain",
  "main": "backend/index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy": "node scripts/deploy.js",
    "setup": "npm run install-deps && npm run deploy",
    "install-deps": "npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "blockchain",
    "voting",
    "solidity",
    "ethereum"
  ],
  "author": "Vinicius Tsuda",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ethers": "^6.8.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.5",
    "solc": "^0.8.21"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
